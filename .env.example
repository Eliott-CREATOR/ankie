# Copy to .env — never commit .env itself.
# No variables needed yet. The C0 review harness runs as a Claude Code subagent
# (.claude/agents/code-reviewer.md), not a scripted API call — nothing here to configure.
